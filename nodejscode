pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh 'npm --version'
            }
        }
    }
}
//npm not found error will get because npm not of this node
