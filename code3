pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'building job'
            }
        }
        stage('approval') {
            steps {
                input 'deploying to prod ? ready ?'
            }
        }
        stage('deploy') {
            steps {
                echo 'stage deploy'
            }
        }
    }
}

